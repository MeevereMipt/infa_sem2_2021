//
// Created by Meevere on 2/26/2021.
//

#include <iostream>

namespace contest2 {

    void repeat_char( char a, int num ){
        for(;num>0;num--)
            std::cout << a;
    }

    // от одного разряда числа
    void cout_babylon( int a ) {
        if( a >= 60 )
            //something went wrong
            return;


    }

    int taskJ() {
        int input, remainder;
        std::cin >> input;

        while(input > 0) {
            remainder = input % 60;


        }
        return 0;
    }

}